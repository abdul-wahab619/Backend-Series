<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tasks Note App</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form text-white p-10">
        <form action="/create" method="post">
          <input
            class="block px-5 py-2 rounded-md bg-zinc-800"
            type="text"
            placeholder="Tile here"
            name="title"
          />
          <textarea
            class="mt-2 block px-5 py-2 resize-none rounded-md bg-zinc-800"
            placeholder="write your task"
            name="details"
          ></textarea>
          <input
            class="mt-2 font-bold bg-green-400 rounded-md px-5 py-2"
            type="submit"
            value="Create Task"
          />
        </form>
      </div>
      <div class="flex flex-wrap gap-4 tasks text-white p-10">
        <% if (files.length > 0) { %> <% files.forEach(function (val) { %>
        <div class="task w-72 rounded-md px-3 py-4 bg-zinc-800">
          <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>
          <a class="text-blue-500 inline-block mt-3" href="/file/<%= val  %>"
            >Read More</a
          >
        </div>
        <% }) %> <% } else { %>
        <h3 class="text-zinc-600">No task yet.</h3>
        <% } %>
      </div>
    </div>

    <script src="/javascripts/script.js"></script>
  </body>
</html>
